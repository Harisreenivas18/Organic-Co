// ============================================================
// PRODUCTION CONFIGURATION
// Set this to your deployed Spring Boot backend URL.
// Example: 'https://organic-co.onrender.com'
// Leave empty string '' if the frontend is served BY the
// Spring Boot app itself (i.e. accessing via the backend URL).
// ============================================================
const API_BASE = 'YOUR_SPRING_BOOT_BACKEND_URL_HERE';
